# @sanity/vision declares a strict react@^18 peer dependency, but this
# project pins a react 19 RC build (see README "Known Issues"). Without
# this, `npm install` fails with ERESOLVE on Vercel, CI, and fresh clones.
legacy-peer-deps=true
